import React from "react";

/**
 * Native stub — returns null; AnimatedWalletSlide is used directly on native.
 * If you see this log on web, Metro is NOT resolving the .web.tsx file.
 */
export default function LottieWalletSlide() {
  if (process.env.NODE_ENV === "development") {
    console.warn("[LottieWalletSlide] WARNING: native stub loaded — .web.tsx not resolving!");
  }
  return null;
}
