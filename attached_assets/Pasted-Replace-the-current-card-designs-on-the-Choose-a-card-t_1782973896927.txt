Replace the current card designs on the "Choose a card type" page with the uploaded card assets.

• First, completely remove the existing Regular and Premium card designs from this page. Do not keep any placeholder, generated, or default card artwork.

• Use the uploaded card assets as the official production card templates:
  - White card → Regular Card
  - Sky Blue card → Premium Card

• These are final assets. Do NOT recreate, redraw, redesign, regenerate, crop, resize disproportionately, stretch, recolor, sharpen, blur, add overlays, change opacity, modify typography, replace logos, alter branding, edit spacing, edit icons, add gradients, add borders, or add shadows directly to the card artwork. Render the uploaded assets exactly as provided.

• Display both cards side by side with identical dimensions while preserving their original aspect ratio. Label them clearly:
  - Regular
  - Premium

• Since the Regular Card is white, place both cards inside a dedicated modern fintech showcase container with:
  - Background color: #F5F7FA
  - Rounded corners
  - Comfortable padding around the cards

This light-gray container exists only to provide contrast for the white card. Do not modify the cards themselves in any way.

• Keep the existing card selection behavior. Users must be able to tap either card to select it. Preserve the current animations, navigation, state management, and business logic.

• When a card is selected, apply the visual selection indicator around the outer card container only. Never place the selection highlight on top of or directly modify the uploaded card artwork.

• Keep everything else on the page unchanged, including layout, typography, spacing outside the showcase section, buttons, navigation, API calls, routing, and card creation flow.

The only visual change should be replacing the current card designs with the uploaded White Regular Card and Sky Blue Premium Card, displayed inside the light-gray (#F5F7FA) showcase container while preserving the assets exactly as provided.